# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Calendar
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: allDay
      external_name: allDay
      # Type: Boolean Stats: 9 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: color
      external_name: color
      # Type: String Stats: 9 properties of this type in this kind.

    - property: createdDate
      external_name: createdDate
      # Type: Date/Time Stats: 9 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: disabled
      external_name: disabled
      # Type: Boolean Stats: 9 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: endDate
      external_name: endDate
      # Type: Date/Time Stats: 9 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: end
      external_name: end
      # Type: String Stats: 9 properties of this type in this kind.

    - property: eventPattern
      external_name: eventPattern
      # Type: String Stats: 9 properties of this type in this kind.

    - property: hit
      external_name: hit
      # Type: Integer Stats: 9 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: id
      external_name: id
      # Type: Integer Stats: 9 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: isRecurring
      external_name: isRecurring
      # Type: Boolean Stats: 9 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: modifiedDate
      external_name: modifiedDate
      # Type: Date/Time Stats: 9 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: movieId
      external_name: movieId
      # Type: Integer Stats: 9 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: startDate
      external_name: startDate
      # Type: Date/Time Stats: 9 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: start
      external_name: start
      # Type: String Stats: 9 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 9 properties of this type in this kind.


- kind: CounterShard_Social
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: count
      external_name: count
      # Type: Integer Stats: 4 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: CounterShard_template
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: count
      external_name: count
      # Type: Integer Stats: 5 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: GaeUserDetails
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: accountNonExpired
      external_name: accountNonExpired
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: accountNonLocked
      external_name: accountNonLocked
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: credentialsNonExpired
      external_name: credentialsNonExpired
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: enabled
      external_name: enabled
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: password
      external_name: password
      # Type: String Stats: 1 properties of this type in this kind.

    - property: userId
      external_name: userId
      # Type: String Stats: 1 properties of this type in this kind.

    - property: userRoles
      external_name: userRoles
      # Type: String Stats: 1 properties of this type in this kind.


- kind: Geniu
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: details
      external_name: details
      # Type: String Stats: 989 properties of this type in this kind.

    - property: frequencyCount
      external_name: frequencyCount
      # Type: Integer Stats: 300 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: lastUpdatedDate
      external_name: lastUpdatedDate
      # Type: Date/Time Stats: 300 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: platformValue
      external_name: platformValue
      # Type: String Stats: 689 properties of this type in this kind.

    - property: platform
      external_name: platform
      # Type: String Stats: 731 properties of this type in this kind.

    - property: what
      external_name: what
      # Type: String Stats: 989 properties of this type in this kind.

    - property: wordCount
      external_name: wordCount
      # Type: Integer Stats: 300 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Huma
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: details
      external_name: details
      # Type: String Stats: 36 properties of this type in this kind.

    - property: frequencyCount
      external_name: frequencyCount
      # Type: Integer Stats: 9 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: lastUpdatedDate
      external_name: lastUpdatedDate
      # Type: Date/Time Stats: 9 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: what
      external_name: what
      # Type: String Stats: 36 properties of this type in this kind.

    - property: wordCount
      external_name: wordCount
      # Type: Integer Stats: 9 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Movie
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: Modified
      external_name: Modified
      # Type: Date/Time Stats: 49 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: Title
      external_name: Title
      # Type: String Stats: 49 properties of this type in this kind.

    - property: URL
      external_name: URL
      # Type: String Stats: 49 properties of this type in this kind.

    - property: calendarId
      external_name: calendarId
      # Type: Integer Stats: 12 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: channelPattern
      external_name: channelPattern
      # Type: String Stats: 36 properties of this type in this kind.

    - property: description
      external_name: description
      # Type: String Stats: 49 properties of this type in this kind.

    - property: eventPattern
      external_name: eventPattern
      # Type: String Stats: 46 properties of this type in this kind.

    - property: id
      external_name: id
      # Type: Integer Stats: 49 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: oid
      external_name: oid
      # Type: String Stats: 47 properties of this type in this kind.

    - property: owner
      external_name: owner
      # Type: String Stats: 47 properties of this type in this kind.

    - property: searchResults
      external_name: searchResults
      # Type: Text Stats: 49 properties of this type in this kind.
      import_transform: db.Text

    - property: shared
      external_name: shared
      # Type: Boolean Stats: 47 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)


- kind: Secure
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: details
      external_name: details
      # Type: String Stats: 246 properties of this type in this kind.

    - property: frequencyCount
      external_name: frequencyCount
      # Type: Integer Stats: 6 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: lastUpdatedDate
      external_name: lastUpdatedDate
      # Type: Date/Time Stats: 6 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: platformValue
      external_name: platformValue
      # Type: String Stats: 240 properties of this type in this kind.

    - property: platform
      external_name: platform
      # Type: String Stats: 243 properties of this type in this kind.

    - property: what
      external_name: what
      # Type: String Stats: 246 properties of this type in this kind.

    - property: wordCount
      external_name: wordCount
      # Type: Integer Stats: 6 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: ServiceRegistry
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: category
      external_name: category
      # Type: String Stats: 448 properties of this type in this kind.

    - property: descriptionText
      external_name: descriptionText
      # Type: Text Stats: 448 properties of this type in this kind.
      import_transform: db.Text

    - property: description
      external_name: description
      # Type: String Stats: 265 properties of this type in this kind.

    - property: disabled
      external_name: disabled
      # Type: Boolean Stats: 448 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: endpoint
      external_name: endpoint
      # Type: String Stats: 448 properties of this type in this kind.

    - property: hit
      external_name: hit
      # Type: Integer Stats: 448 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: lastAccessed
      external_name: lastAccessed
      # Type: Date/Time Stats: 272 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: lastUpdated
      external_name: lastUpdated
      # Type: Date/Time Stats: 448 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: saveOnly
      external_name: saveOnly
      # Type: Boolean Stats: 448 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: service
      external_name: service
      # Type: String Stats: 448 properties of this type in this kind.

    - property: shortUrl
      external_name: shortUrl
      # Type: String Stats: 170 properties of this type in this kind.

    - property: summary
      external_name: summary
      # Type: String Stats: 3 properties of this type in this kind.

    - property: useDescription
      external_name: useDescription
      # Type: Boolean Stats: 448 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: useHtml
      external_name: useHtml
      # Type: Boolean Stats: 448 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)


- kind: Settings
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: pinCode
      external_name: pinCode
      # Type: String Stats: 1 properties of this type in this kind.

    - property: recorded
      external_name: recorded
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: setting
      external_name: setting
      # Type: String Stats: 1 properties of this type in this kind.

    - property: status
      external_name: status
      # Type: String Stats: 1 properties of this type in this kind.


- kind: User
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: id
      external_name: id
      # Type: Integer Stats: 5 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: name
      external_name: name
      # Type: String Stats: 5 properties of this type in this kind.


- kind: _AE_Backup_Information
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: blob_files
      external_name: blob_files
      # Type: String Stats: 1056 properties of this type in this kind.

    - property: completed_jobs
      external_name: completed_jobs
      # Type: String Stats: 31 properties of this type in this kind.

    - property: kinds
      external_name: kinds
      # Type: String Stats: 31 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 1 properties of this type in this kind.

    - property: start_time
      external_name: start_time
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: _AE_DatastoreAdmin_Operation
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: active_job_ids
      external_name: active_job_ids
      # Type: String Stats: 19 properties of this type in this kind.

    - property: active_jobs
      external_name: active_jobs
      # Type: Integer Stats: 3 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: completed_jobs
      external_name: completed_jobs
      # Type: Integer Stats: 3 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: description
      external_name: description
      # Type: Text Stats: 3 properties of this type in this kind.
      import_transform: db.Text

    - property: last_updated
      external_name: last_updated
      # Type: Date/Time Stats: 3 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: status
      external_name: status
      # Type: String Stats: 3 properties of this type in this kind.


- kind: _ah_SESSION
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: _expires
      external_name: _expires
      # Type: Integer Stats: 1893 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: _values
      external_name: _values
      # Type: Blob Stats: 1893 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode


- kind: com.onpositive.traps.CallStorage
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: data
      external_name: data
      # Type: Blob Stats: 6 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode


- kind: content
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: data
      external_name: data
      # Type: Blob Stats: 132 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode


- kind: meta
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: modified
      external_name: modified
      # Type: Integer Stats: 50 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: size
      external_name: size
      # Type: Integer Stats: 50 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

