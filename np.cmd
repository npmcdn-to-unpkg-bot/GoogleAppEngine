git status
git log --branches --not --remotes
git config user.name
git config user.email
git branch
echo make sure cache proxy is up and running
lsof -P -n -i4TCP:8080 | grep LISTEN
echo make sure gae app server is up and running
lsof -P -n -i4TCP:8888 | grep LISTEN
